{
  "name": "@chat/utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsc -w",
    "lint": "eslint src --ext .ts --config ../configs/eslint/base.cjs",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "prettier": "^3.3.3",
    "rimraf": "^5.0.10",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
